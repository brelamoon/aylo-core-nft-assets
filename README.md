# AYLO Core NFT Assets

Public image and metadata files for AYLO Core NFTs.

This repository contains public NFT assets only. It must never contain wallet keys, mint authority keys, treasury keys, API tokens, private scripts, or `.env` files.
